(function () {
    'use strict';

    angular.module('ssWidgets')
        .component('ssWeather', component);

    var component = {
        controller: Controller
    };

    function Controller() {

    }

})();